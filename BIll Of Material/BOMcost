Calculate_BOM_Cost as "Calculate BOM Cost"
{
	type =  form
	form = Bill_of_Material
	record event = on add or edit

	on user input of List_Of_Items.Amount
	{
		actions 
		{
			custom deluge script
			(
					// Sum = 0;
				// for each  r in input.List_Of_Items
				// {
				// 	Sum = Sum + ifnull(r.Amount,0);
				// }
				input.BOM_Cost = input.BOM_Cost + row.Amount;
			)
		}
	}

}

